apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: antwort
spec:
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: antwort
    weight: 100
  port:
    targetPort: http
