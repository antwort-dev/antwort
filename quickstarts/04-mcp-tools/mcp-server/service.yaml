apiVersion: v1
kind: Service
metadata:
  name: mcp-test-server
  labels:
    app.kubernetes.io/component: mcp-test-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/component: mcp-test-server
