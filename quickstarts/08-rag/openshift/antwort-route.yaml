apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: antwort
spec:
  to:
    kind: Service
    name: antwort
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
