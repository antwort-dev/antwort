apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - antwort-serviceaccount.yaml
  - antwort-configmap.yaml
  - antwort-deployment.yaml
  - antwort-service.yaml
  - antwort-pvc.yaml
  - qdrant-deployment.yaml
  - qdrant-service.yaml
  - docling-deployment.yaml
  - docling-service.yaml
  - embedding-deployment.yaml
  - embedding-service.yaml

commonLabels:
  app.kubernetes.io/part-of: antwort-rag
